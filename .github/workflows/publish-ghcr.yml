name: Publish Docker Images to GHCR

on:
  push:
    branches:
        -   main

jobs:
    publish:
        runs-on: ubuntu-latest

        permissions:
            contents: read
            packages: write

        strategy:
            fail-fast: false
            matrix:
                include:
                -   tag: jre8
                    base_image: debian:stable-slim
                    java: 8
                    opencl: false

                -   tag: jre11
                    base_image: debian:stable-slim
                    java: 11
                    opencl: false

                -   tag: jre17
                    base_image: debian:stable-slim
                    java: 17
                    opencl: false

                -   tag: jre21
                    opencl: false

                -   tag: jre25
                    base_image: debian:stable-slim
                    java: 25
                    opencl: false

                -   tag: jre25-gpu
                    base_image: nvidia/cuda:13.0.0-runtime-debian12
                    java: 25
                    opencl: true
        steps:
            -   name: Checkout
                uses: actions/checkout@v4

            # -----------------------------------
            # Docker Buildx
            # -----------------------------------
            -   name: Set up Docker Buildx
                uses: docker/setup-buildx-action@v3

            # -----------------------------------
            # Login to GHCR
            # -----------------------------------
            -   name: Login to GHCR
                uses: docker/login-action@v3
                with:
                    registry: ghcr.io
                    username: ${{ github.actor }}
                    password: ${{ secrets.GITHUB_TOKEN }}

            # -----------------------------------
            # Build & Push
            # -----------------------------------
            -   name: Build and push image
                uses: docker/build-push-action@v6
                with:
                    context: .
                    file: ./Dockerfile
                    push: true
                    tags: |
                        ghcr.io/alexandergg-0520/minecraft-server:${{ matrix.tag }}
                    build-args: |
                        JAVA_VERSION=${{ matrix.java }}
                        ENABLE_OPENCL=${{ matrix.opencl }}
