# Vanilla Server Type

This folder contains type-specific overrides for Vanilla Minecraft servers.

Files here will be merged into /data during container startup:

- server.properties
- mc.args
- jvm.args
